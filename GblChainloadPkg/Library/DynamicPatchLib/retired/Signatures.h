#ifndef DPL_UNIVERSAL_SIGNATURES_H_
#define DPL_UNIVERSAL_SIGNATURES_H_

/* kEfispUtf16Pattern and other shared bytes live in the canonical source;
   include it here so EDK2 consumers see the same definition as host tools. */
#include "../../../../tools/shared/patch_signatures.h"

/* No universal-only signatures at this time. */

#endif /* DPL_UNIVERSAL_SIGNATURES_H_ */
