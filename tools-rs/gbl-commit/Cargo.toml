[package]
name = "gbl-commit"
version = "0.0.0"            # real version injected at build from //VERSION
edition.workspace = true
publish.workspace = true
license.workspace = true

[[bin]]
name = "gbl-commit"
path = "src/main.rs"

[dependencies]
libc = "0.2"

[build-dependencies]
cc = "1"
